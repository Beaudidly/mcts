#include "mcts.h"
#include "mcts_node.h"
#include <stdint.h>

// Need to play with inline functions to lower stack overhead
void*
bestMove(State_s* state, uint64_t duration, double c) {
    // Generate a root given the state
    (void) state;
    (void) duration;
    (void) c;


    return NULL;
}
